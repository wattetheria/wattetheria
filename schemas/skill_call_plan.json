{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "SkillCallPlan",
  "type": "object",
  "required": ["skill_id", "input", "required_caps", "estimated_cost", "risk_level", "rationale"],
  "properties": {
    "skill_id": {"type": "string"},
    "input": {"type": "object"},
    "required_caps": {
      "type": "array",
      "items": {"type": "string"}
    },
    "estimated_cost": {"type": "integer"},
    "risk_level": {
      "type": "string",
      "enum": ["low", "medium", "high"]
    },
    "rationale": {"type": "string"}
  },
  "additionalProperties": false
}
